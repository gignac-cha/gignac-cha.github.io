{
  "name": "footprint",
  "version": "0.0.0-20260806.0",
  "description": "Tiny page-access footprint tracker — one step() call, sendBeacon transport, endpoint resolved from browser storage.",
  "type": "module",
  "types": "./outputs/index.d.ts",
  "exports": {
    ".": {
      "types": "./outputs/index.d.ts",
      "import": "./outputs/index.js"
    },
    "./step": {
      "types": "./outputs/footprint.d.ts",
      "import": "./outputs/footprint.js"
    }
  },
  "scripts": {
    "build": "tsc && esbuild index.ts --bundle --platform=browser --target=esnext --format=esm --outfile=outputs/footprint.bundle.js && esbuild index.ts --bundle --platform=browser --target=es2020 --format=cjs --outfile=outputs/index.cjs && esbuild footprint.ts --bundle --platform=browser --target=es2020 --format=cjs --outfile=outputs/footprint.cjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:browser": "pnpm build && playwright test",
    "publish:dry-run": "FOOTPRINT_PUBLISH=0 node --env-file-if-exists=.env publish.ts",
    "publish:release": "FOOTPRINT_PUBLISH=1 node --env-file-if-exists=.env publish.ts"
  },
  "author": "Tes Cha",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
